/** Fixed services hero height in design pixels (Figma). */
export const SERVICES_HERO_HEIGHT = 1584;

/** Footer ray transition below hero — matches portfolio canvas delta. */
export const SERVICES_FOOTER_RAY_ZONE = 511;

/** Baked decor stack (hero gradients + decor + footer ray) in design pixels. */
export const SERVICES_DECOR_BAKE_WIDTH = 1440;
export const SERVICES_DECOR_BAKE_HEIGHT = SERVICES_HERO_HEIGHT + SERVICES_FOOTER_RAY_ZONE;
export const SERVICES_DECOR_BAKE_SRC = '/services/decor-stack.webp';
