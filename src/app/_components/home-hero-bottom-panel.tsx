/** Figma Rectangle 17399 — lower hero frosted glass panel behind stats. */
export function HomeHeroBottomPanel(): React.JSX.Element {
  return <div className="home-hero-bottom-panel" aria-hidden />;
}
